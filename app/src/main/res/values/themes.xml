<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme - Play Store inspired -->
    <style name="Base.Theme.RepoStore" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryVariant">@color/primary_variant</item>
        <item name="colorSecondary">@color/secondary</item>
        <item name="android:statusBarColor">?attr/colorSurface</item>
        <item name="android:navigationBarColor">?attr/colorSurface</item>
        <item name="android:windowLightStatusBar">true</item>

        <!-- Component styles -->
        <item name="toolbarStyle">@style/Widget.RepoStore.Toolbar</item>
        <item name="tabStyle">@style/Widget.RepoStore.TabLayout</item>
        <item name="bottomNavigationStyle">@style/Widget.RepoStore.BottomNavigationView</item>
    </style>

    <style name="Theme.RepoStore" parent="Base.Theme.RepoStore" />

    <!-- Toolbar Style -->
    <style name="Widget.RepoStore.Toolbar" parent="Widget.Material3.Toolbar">
        <item name="titleTextColor">?attr/colorOnSurface</item>
        <item name="navigationIconTint">?attr/colorOnSurface</item>
        <item name="titleTextAppearance">@style/TextAppearance.RepoStore.Toolbar.Title</item>
    </style>

    <style name="TextAppearance.RepoStore.Toolbar.Title" parent="TextAppearance.Material3.TitleLarge">
        <item name="android:textSize">20sp</item>
        <item name="android:textStyle">normal</item>
    </style>

    <!-- Play Store style scrollable category tabs: short rounded indicator,
         medium-weight labels, no all-caps. -->
    <style name="Widget.RepoStore.TabLayout" parent="Widget.Material3.TabLayout">
        <item name="tabMode">scrollable</item>
        <item name="tabGravity">start</item>
        <item name="tabIndicator">@drawable/tab_indicator_rounded</item>
        <item name="tabIndicatorHeight">3dp</item>
        <item name="tabIndicatorColor">?attr/colorPrimary</item>
        <item name="tabIndicatorFullWidth">false</item>
        <item name="tabIndicatorAnimationMode">elastic</item>
        <item name="tabSelectedTextColor">?attr/colorPrimary</item>
        <item name="tabTextColor">?attr/colorOnSurfaceVariant</item>
        <item name="tabRippleColor">?attr/colorPrimary</item>
        <item name="tabTextAppearance">@style/TextAppearance.RepoStore.Tab</item>
        <item name="tabPaddingStart">12dp</item>
        <item name="tabPaddingEnd">12dp</item>
    </style>

    <style name="TextAppearance.RepoStore.Tab" parent="TextAppearance.Material3.TitleSmall">
        <item name="android:textAllCaps">false</item>
        <item name="android:textSize">14sp</item>
    </style>

    <!-- Play Store keeps every bottom-nav label visible and shows a pill
         behind the active icon. -->
    <style name="Widget.RepoStore.BottomNavigationView" parent="Widget.Material3.BottomNavigationView">
        <item name="labelVisibilityMode">labeled</item>
        <item name="itemActiveIndicatorStyle">
            @style/Widget.RepoStore.BottomNavigationView.ActiveIndicator
        </item>
        <item name="itemIconSize">24dp</item>
        <item name="elevation">3dp</item>
    </style>

    <style name="Widget.RepoStore.BottomNavigationView.ActiveIndicator"
        parent="Widget.Material3.BottomNavigationView.ActiveIndicator">
        <item name="android:width">64dp</item>
        <item name="android:height">32dp</item>
        <item name="marginHorizontal">4dp</item>
        <item name="shapeAppearance">@style/ShapeAppearance.Material3.Corner.Full</item>
    </style>

    <!-- Section header title used by the home/games carousels. -->
    <style name="TextAppearance.RepoStore.SectionTitle" parent="TextAppearance.Material3.TitleMedium">
        <item name="android:textSize">16sp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!-- Play Store renders app icons as rounded squares (~22% radius),
         never circles. Circles are reserved for developer avatars. -->
    <style name="ShapeAppearance.App.AppIcon" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">22%</item>
    </style>

    <!-- Circle Image Style - developer/user avatars only -->
    <style name="CircleImageView" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
    </style>

    <!-- Fullscreen Theme for Screenshot Viewer -->
    <style name="Theme.RepoStore.Fullscreen" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowFullscreen">true</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <!-- Split Button Shape Styles -->
    <style name="ShapeAppearance.App.LeftSplitButton" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSizeTopLeft">24dp</item>
        <item name="cornerSizeBottomLeft">24dp</item>
        <item name="cornerSizeTopRight">6dp</item>
        <item name="cornerSizeBottomRight">6dp</item>
    </style>

    <style name="ShapeAppearance.App.RightSplitButton" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSizeTopLeft">6dp</item>
        <item name="cornerSizeBottomLeft">6dp</item>
        <item name="cornerSizeTopRight">24dp</item>
        <item name="cornerSizeBottomRight">24dp</item>
    </style>
</resources>
